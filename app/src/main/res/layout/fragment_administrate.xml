<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="raistudio.testandpractice.AdministrateFragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/administrate_message"
        android:textSize="@dimen/normal_text_size"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/element_spacing"
        android:id="@+id/administrate_message_view"/>

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/element_spacing"
        android:layout_below="@id/administrate_message_view"
        android:id="@+id/administrate_recycler_view"
        android:scrollbars="vertical">

    </android.support.v7.widget.RecyclerView>

    <ImageButton
        android:id="@+id/administrate_select_button"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:background="@android:color/holo_orange_dark"
        android:src="@drawable/ic_check_black_24dp" />

</RelativeLayout>
